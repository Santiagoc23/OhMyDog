<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <style>
        /*
 * Globals
 */


/* Custom default button */
.btn-light,
.btn-light:hover,
.btn-light:focus {
  color: #333;
  text-shadow: none; /* Prevent inheritance from `body` */
}

h3{
    color: rgba(242,116, 5, .95);
     border-bottom: .25rem solid transparent;
}


/*
 * Base structure
 */
    body, html {
    height: 100%;
    }

    body {
    text-shadow: 0 .05rem .1rem rgba(0, 0, 0, .5);
    box-shadow: inset 0 0 5rem rgba(0, 0, 0, .5);
    }

    .cover-container {
    min-height: 100%;
    display: flex;
    flex-direction: column;
    max-width: 42em;
    }


/*
 * Header
 */

.nav-masthead .nav-link {
  color: rgba(242,116, 5, .95);
  border-bottom: .25rem solid transparent;
}

.nav-masthead .nav-link:hover,
.nav-masthead .nav-link:focus {
  border-bottom-color: rgba(242,116, 5, .95);
}

.nav-masthead .nav-link + .nav-link {
  margin-left: 1rem;
}

.nav-masthead .active {
  color: #fff;
  border-bottom-color: #fff;
}
    </style>
  </head>

   <body class="d-flex h-100 text-center text-bg-dark"> 
        <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
            <header class="mb-auto">
                <div>
                    <h3 class="float-md-start mb-0">OhMyDog</h3>
                    <nav class="nav nav-masthead justify-content-center float-md-end">
                       <!-- <a class="nav-link fw-bold py-1 px-0 active" aria-current="page" href="#">Inicio</a>-->
                        <%= link_to "Inicio", root_path, class:"nav-link fw-bold py-1 px-0 active"  %>
                        <%= link_to "Adopción", adoptions_path, class:"nav-link fw-bold py-1 px-0"  %>
                        <%= link_to "Cuidadores", index_user_caregiver_path, class:"nav-link fw-bold py-1 px-0"  %>
                        <%= link_to "Paseadores", index_user_walker_path, class:"nav-link fw-bold py-1 px-0"  %>
                        <%= link_to "Donaciones", donations_path, class:"nav-link fw-bold py-1 px-0"  %>

                    </nav>
                </div>
            </header>

            <main class="px-3">
               <%= yield %>
            </main>

            <footer class="mt-auto text-white-50">
            </footer>
        </div>

    </body>
</html>