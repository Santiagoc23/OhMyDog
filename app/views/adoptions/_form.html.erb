<div class=""> 
  <div class="container">
    <div class="d-flex justify-content-center align-items-center mt-4">
      <div class="col-md-7 col-lg-8">
        <h4 class="mb-3">Datos del perro</h4>
        <%= form_with(model: adoption) do |f| %>
          <div class="row g-3">
            <div class="col-sm-6">
               <%= f.label :Nombre, class:"form-label" %>
               <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"form-control", placeholder:"", value:"", required: "required"%>
            </div>

            <div class="col-sm-6">
               <%= f.label :Raza, class:"form-label" %>
               <%= f.text_field :race, autofocus: true, autocomplete: "race", class:"form-control", placeholder:"", value:"", required: "required"%>
            </div>
              
            <div class="col-12">
               <%= f.label :Tamaño, class:"form-label" %>
               <%= f.text_field :size, autofocus: true, autocomplete: "size", class:"form-control", placeholder:"", value:"", required: "required"%>
            </div>
            
            <div class="col-12">
              <%= f.label :Sexo, class:"form-label" %>
              <%= f.text_field :sex, autofocus: true, autocomplete: "sex", class:"form-control", placeholder:"", value:"", required: "required"%>
            </div>

            <div class="col-12">
              <%= f.label :'Descripción (Personalidad, apariencia)', class:"form-label" %>
              <%= f.text_area :description, autofocus: true, autocomplete: "description", class:"form-control", placeholder:"", value:"", required: "required"%>
            </div>

            <div class="col-12">
              <%= f.label :Situación, class:"form-label" %>
              <%= f.text_area :situation, autofocus: true, autocomplete: "situation", class:"form-control", placeholder:""  %>
            </div>
          </div>

          <% if flash[:alert] %>
            <div class="alert alert-danger my-2 p-1 text-center">
              <%= flash[:alert] %>
            </div>
          <% end %>   

          <% if adoption.errors.any? %>
            <div class="alert alert-danger my-2 p-1 text-center">
              <ul>
                <% adoption.errors.each do |error| %>
                  <li><%= error.full_message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <hr class="mt-4">
          <%= f.submit "Publicar adopción", class:"w-100 btn btn-primary btn-lg" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
